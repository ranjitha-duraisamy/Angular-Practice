import { BackgroundStyleDirective } from './background-style.directive';

describe('BackgroundStyleDirective', () => {
  it('should create an instance', () => {
    const directive = new BackgroundStyleDirective();
    expect(directive).toBeTruthy();
  });
});
